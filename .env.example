# FICTIONAL EXAMPLE — not a real 1Password vault or machine.
# Optional: for scripts only. Prefer op:// references in templates.
# Never put real values here in a commit.
OP_VAULT=Example-Vault
MACHINE_NAME=example-machine
